# Secrets
.env
credentials.json
token.json
*.pem

# Temp / intermediates
.tmp/
!.tmp/.gitkeep

# NOTE: docs/ is generated by tools/site_build.py but is deliberately NOT ignored —
# GitHub Pages serves the live site straight from it. Source of truth is still
# content/ + templates/; never hand-edit anything in docs/.

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# Private business plan — deliberately NOT published. This repo is public.
business/
